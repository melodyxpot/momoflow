# MomoLinks production reverse proxy
# Two hostnames, TLS via Let's Encrypt (webroot challenge served from certbot-www).

upstream momolinks_api { server api:4000 keepalive 64; }
upstream momolinks_web { server web:3000 keepalive 32; }

# ─── HTTP → HTTPS (and ACME challenge) ───────────────────────────────────────
server {
    listen 80;
    listen [::]:80;
    server_name momolinks.io app.momolinks.io;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# ─── momolinks.io  →  short-link redirects + REST API ─────────────────────────
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name momolinks.io;

    ssl_certificate     /etc/letsencrypt/live/momolinks.io/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/momolinks.io/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;

    # Redirect path should be fast.
    proxy_read_timeout 5s;
    proxy_send_timeout 5s;

    location / {
        proxy_pass         http://momolinks_api;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Connection        "";
    }
}

# ─── app.momolinks.io  →  Next.js dashboard ───────────────────────────────────
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name app.momolinks.io;

    ssl_certificate     /etc/letsencrypt/live/momolinks.io/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/momolinks.io/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;

    location / {
        proxy_pass         http://momolinks_web;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Connection        "";
    }
}
