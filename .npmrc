# HeroUI requires its packages to be hoisted so Tailwind can scan them
# (without this, classes from @heroui/* are missing in production builds).
public-hoist-pattern[]=*@heroui/*

# Optional: avoid peer-dep noise from HeroUI/framer-motion
strict-peer-dependencies=false
auto-install-peers=true
